- Change parameters of function so people understand better each.

- Change parameter it (used for the set.seed()) in order to be an optional parameter (it = NULL).

- Add different ways to initialize the centroids (random, k-means...).

- Add different ways to compute the standard deviations of each RBF (the same for all, different for each one...).

- Change stds[1] for stds in hiddenLayer (change .cpp file).

- Extend to multiple tasks: regression, binary classification...

- Group centroids, stds, model in one object.

- Example in predict RBFNN for other input types (distance and Gaussian kernel).

- Write TODOs in DESCRIPTION file and man/RBFNN-package.Rd.

- Add tests for the functions.